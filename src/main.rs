use simple_stock_tracking::app;

fn main() {
    // 1. Get command line parameters
    let matches = app::build().get_matches();

    // 2. Validate parameters
    // 3. Ingest stock quote data from API
    // 4. Calculate performance indicators
    // 5. print CSV
}
